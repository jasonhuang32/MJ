# MJ Edge 浏览器动效扩展 — 产品与技术规格

版本：0.1（个人本地原型）  
日期：2026-08-26  
目标浏览器：Microsoft Edge 桌面版

## 1. 产品目标

制作一个本地安装的 Edge 扩展。用户在普通网页的可编辑区域输入 `mj`（不区分大小写）时，网页上方立即出现与 `sample` 目录样例一致的卡通蜘蛛侠摆荡动效，并同步播放样例中的 “Hi, my name is Peter Parker” 音频。输入内容保持不变，每次输入均触发播放。

第一阶段仅供个人本地使用，不提交扩展商店，不收集或上传任何网页内容。

## 2. 样例基准

基准文件：

- `sample/455c91bac92e9529392dae1c040d961f.mp4`：8.24 秒，576×1280、30 fps；动效/声音主体约 2.31 秒。画面为倒挂的大头蜘蛛侠，从屏幕顶部进入，在视口上半区摆荡后向顶部退出。
- `sample/6d74a0c7968496267fb90ee39e7b8975.mp4`：7.30 秒，576×1280、30 fps；动效/声音主体约 2.91 秒。画面为全身蜘蛛侠，悬挂蛛丝从顶部进入，在视口上半区左右摆荡，伴随粉色爱心，最后向左上方退出。

两段样例视为同一 `mj` 特效的两个视觉变体。每次触发时等概率随机播放其中一个变体，且为每个变体使用各自样例中的对应音轨。该随机播放方式已经用户确认。

## 3. 功能范围

### 3.1 支持的触发区域

扩展应在普通网页中监听以下可编辑区域：

- `input[type=text]`
- `input[type=search]`
- 未声明 `type` 的文本输入框
- `textarea`
- `contenteditable` 元素及其可编辑后代
- 由 React、Vue 等前端框架动态创建的上述元素
- 抖音网页版搜索栏、评论输入区等符合上述条件的动态编辑区域

明确排除：

- 密码框
- 只读或禁用的输入框
- 非文本控件，例如日期、数字、颜色、文件选择器
- 浏览器地址栏、侧边栏、设置页、扩展商店页
- `edge://`、`chrome://`、浏览器内置 PDF 阅读器等禁止内容脚本运行的受保护页面
- 扩展没有访问权限的跨域 iframe；有权限且允许注入的 iframe 可以支持

### 3.2 触发规则

1. 当一次用户输入使光标前最近两个字符组成 `mj` 时触发。
2. 匹配不区分大小写，因此 `mj`、`MJ`、`Mj`、`mJ` 均触发。
3. `mj` 可以独立出现，也可以是更长内容的一部分，例如 `hellomj`。
4. 输入法组合期间不提前触发，在 `compositionend` 后检查最终文本。
5. 每一个新形成的 `mj` 只触发一次；输入 `mjmj` 应连续触发两次。
6. 删除后重新输入 `mj` 应再次触发。
7. 页面已有的 `mj`、仅聚焦输入框或移动光标不得触发。
8. 原型默认键盘输入、软键盘输入和输入法提交均可触发；粘贴或脚本批量赋值默认不触发。
9. 扩展不得删除、替换、提交或以其他方式修改用户输入，`mj` 必须原样保留。

### 3.3 连续触发

- 不设置冷却时间。
- 如果上一段动画尚未结束又检测到新的 `mj`，立即启动新一轮播放。
- 为避免多个大图层无限叠加，同时存在的实例上限为 3；超过上限时结束最早的实例，再播放最新实例。
- 每次触发独立播放一次音频，不主动阻止短时间内的音频叠加。

## 4. 动效表现

### 4.1 通用表现

- 动效以透明覆盖层呈现在网页最上方，覆盖层相对浏览器可视区域固定，不随页面滚动。
- 覆盖层不遮断鼠标、触摸、选择、滚动或键盘操作，必须设置为不可交互。
- 不改变网页布局，不插入占位空间，不移动输入框，不造成页面跳动。
- 角色从视口顶部之外入场，主体活动范围集中在视口上半区；轨迹、旋转、缩放、蛛丝、爱心和退场方向以对应样例逐帧为准。
- 桌面宽屏下保持样例的视觉比例，但设置合理最大尺寸，避免角色被放大到占满整个桌面。建议角色宽度约为视口宽度的 38%–48%，最大 520 CSS px；窄窗口按比例缩小。
- 动效以 60 fps 渲染为目标；在不改变视觉节奏的前提下，最低应保持流畅的 30 fps。
- 动画结束后彻底移除该实例的 DOM、计时器和媒体对象。

### 4.2 变体 A：倒挂大头版

- 使用样例一中的红色圆形大头、白色眼罩、黑色描边及顶部小型身体造型。
- 从顶部进入，头部存在明显旋转/翻转和横向摆动。
- 主体在视口上半区移动，不下落到输入框附近。
- 最终向顶部收回并消失。
- 目标时长约 2.31 秒，以提取后的有效素材时长为准。

### 4.3 变体 B：全身爱心版

- 使用样例二中的红蓝全身卡通蜘蛛侠、黑色蛛丝和粉色爱心。
- 从顶部偏右区域进入，随后围绕顶部蛛丝锚点左右摆荡。
- 角色经过中上部区域，移动中伴随轻微姿态变化；粉色爱心跟随出现。
- 最终向左上方离开视口。
- 目标时长约 2.91 秒，以提取后的有效素材时长为准。

## 5. 音频

- 从两段样例各自提取有效音频段，裁掉开头和结尾静音，分别绑定对应动画变体。
- 播放内容以样例中的 “Hi, my name is Peter Parker” 原声及其伴随音效为准，不另外配音。
- 动画首帧出现与音频有效段开始应同步，允许误差不超过 80 ms。
- 音量以样例相对响度为基础，不修改系统音量；不得突然放大到明显高于普通网页媒体的水平。
- 音频失败不得阻止动画播放。若 Edge 的自动播放策略在特定页面阻止声音，扩展应保持动画正常，并在扩展图标中提供简短状态提示或重新启用声音的入口。
- 扩展默认开启声音；第一阶段可提供一个简单开关，用于临时静音全部特效。

## 6. 素材处理方案

### 6.1 当前 0.4 版实现

- 使用 `sample/sample_better.mp4` 中的高清画面作为重制参考。
- 大头倒挂版使用完整面罩内层，白色眼片与黑色眼框保持素材原始配准。
- 全身版使用三张完整的 2048×2048 透明姿势图，按样例动作阶段切换；不再使用原视频中被画面边缘裁切的姿势帧。
- 原视频中的灰色背景、水印和大部分原始绳线不进入网页覆盖层。
- 蛛丝由扩展运行时使用内联 SVG 绘制。
- 各姿势按共同的头顶蛛丝锚点配准，SVG 长蛛丝延伸进角色轮廓，摆动中不会露出连接缝隙。
- 角色与爱心素材在播放前预加载并完成解码，降低首次触发时的卡顿和缺帧。
- 全身版的粉色爱心使用独立 SVG，在对应伸手姿势中从右手指尖连续发射。
- 入场、摆荡、细微晃动和退场均由 Web Animations 实现，不再使用逐帧 APNG。

### 6.2 原始素材恢复原则

由于当前只有带页面背景的屏幕录像，开发阶段采用以下流程：

1. 从每段样例定位动效有效帧和有效音频区间。
2. 使用动效出现前的静态页面帧作为背景参考，对角色、蛛丝和爱心做背景差分与透明抠图。
3. 对压缩边缘、半透明抗锯齿和被页面细节污染的区域进行清理。
4. 将视觉帧打包为带透明通道、适合 Edge 播放的本地动画素材；优先考虑透明 WebM，必要时使用压缩 PNG 帧序列。
5. 将对应音轨存为扩展内置本地音频，不依赖网络。
6. 对照原视频逐帧校准尺寸、位置、旋转和音画同步。

如果屏幕录像无法恢复出干净边缘，则依据样例重绘透明角色素材，并保持形象、构图、摆荡节奏和音画体验尽可能一致；不直接把整段手机录屏覆盖到网页上。

## 7. 扩展实现约束

- 使用 Edge 支持的 Manifest V3。
- 通过内容脚本在获准访问的网页中监听真实用户输入。
- 输入监听使用捕获阶段，并兼容动态 DOM、输入法组合和 `contenteditable`。
- 动效挂载到隔离的 Shadow DOM 容器，避免网页 CSS 污染特效，也避免扩展样式影响网页。
- 覆盖层使用视口固定定位、最高层级和 `pointer-events: none`。
- 所有图片、动画和音频均打包在扩展本地；运行时不请求外部服务器。
- 不读取、不保存、不记录、不上传输入框的完整内容；检测逻辑只在内存中检查光标附近为识别 `mj` 所需的最少字符。
- 默认请求普通网页所需权限；不申请历史记录、剪贴板、Cookie、摄像头、麦克风等无关权限。

## 8. 设置项（第一阶段最小范围）

扩展图标弹窗仅提供：

- 总开关：开启/暂停 `mj` 特效
- 声音开关：开启/静音
- 变体模式：随机 / 仅大头倒挂版 / 仅全身爱心版

设置保存在 Edge 本地扩展存储中，不进行云同步。

## 9. 验收标准

### 9.1 触发正确性

- 在 Edge 普通网页的搜索框、文本框、文本域和 `contenteditable` 中分别输入四种大小写组合，均能触发。
- 在抖音网页版搜索栏和评论区输入 `mj` 能触发，且不影响正常输入和发送评论。
- 密码框、只读框和非编辑区域不触发。
- 输入内容中的 `mj` 保留不变。
- 连续两次输入能够触发两次；删除后重输能够再次触发。
- 中文输入法组合过程中不误触发。

### 9.2 视觉与声音

- 两个变体的角色形象、颜色、蛛丝/爱心元素、入场点、摆荡路径、旋转、停留时间和退场方式与样例肉眼对照一致。
- 角色始终覆盖在网页之上，网页布局和滚动位置不变，页面仍可操作。
- 动画与对应音频同步误差不超过 80 ms。
- 动画结束后页面中无残留覆盖层。

### 9.3 稳定性

- 在普通网站和抖音这类单页应用中切换页面后仍可触发。
- 快速连续触发 20 次后无明显内存持续增长、页面卡死或永久残留音频。
- 页面自身存在高层级弹窗、复杂 CSS 或前端框架时，特效仍能正常显示且不污染页面样式。

## 10. 非目标与已知边界

- 第一阶段不支持浏览器地址栏和 Edge 内置页面。
- 第一阶段不做移动端 Edge 或手机系统级输入法特效。
- 第一阶段不提交扩展商店，也不制作对外隐私政策和商店宣传素材。
- 当前样例中的蜘蛛侠形象和影视原声仅按用户要求用于个人本地原型。若未来公开分发、商业使用或上架商店，发布前必须重新评估素材授权，并视情况取得许可或更换为可发布素材。

## 11. 开发完成物

- 可通过 Edge“加载解压缩的扩展”安装的完整扩展目录
- 从样例处理得到的两套本地动效素材与对应音频
- 简洁的设置弹窗
- 安装与使用说明
- 手工验收清单
