{
  "name": "mj-edge-effect",
  "version": "0.2.0",
  "private": true,
  "description": "Edge extension that plays the supplied MJ Spider-Man effects in editable fields.",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "validate": "node scripts/validate-extension.mjs",
    "check": "node --test tests/*.test.cjs && node scripts/validate-extension.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
